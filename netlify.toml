[build]
  base = "client"
  command = "npm ci && npm run build"
  publish = "dist"

# SPA routing: any route should serve index.html (wouter needs this)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
